import { PostLayout } from "@/components/post-layout";

export default PostLayout({
  title: "A component library without the hard parts",
  description: "Using headless UI. Something something...",
  draft: true,
});
